{{ printf "<?xml version=\"1.0\" encoding=\"utf-8\" standalone=\"yes\"?>" | safeHTML }}
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9"
  xmlns:xhtml="http://www.w3.org/1999/xhtml">
{{ range where .Pages "Sitemap.Disable" "ne" true }}
  {{- if .Permalink -}}
  {{- $lastmod := .Lastmod -}}
  {{- if eq .Type "timeline" -}}
    {{- $dailyPages := slice -}}
    {{- range .Site.RegularPages -}}
      {{- if and .File (findRE "^[0-9]{4}-[0-9]{2}$" .Section) (findRE "^[0-9]{4}-[0-9]{2}-[0-9]{2}$" .File.BaseFileName) -}}
        {{- $dailyPages = $dailyPages | append . -}}
      {{- end -}}
    {{- end -}}
    {{- $dailyPages = sort $dailyPages "File.Path" "desc" -}}
    {{- if gt (len $dailyPages) 0 -}}
      {{- $latestDaily := index (first 1 $dailyPages) 0 -}}
      {{- $lastmod = $latestDaily.Date -}}
    {{- end -}}
  {{- end -}}
  <url>
    <loc>{{ .Permalink }}</loc>{{ if not $lastmod.IsZero }}
    <lastmod>{{ safeHTML ( $lastmod.Format "2006-01-02T15:04:05-07:00" ) }}</lastmod>{{ end }}{{ with .Sitemap.ChangeFreq }}
    <changefreq>{{ . }}</changefreq>{{ end }}{{ if ge .Sitemap.Priority 0.0 }}
    <priority>{{ .Sitemap.Priority }}</priority>{{ end }}{{ if .IsTranslated }}{{ range .Translations }}{{ if and (not .Sitemap.Disable) (not (.Params.noindex | default false)) }}
    <xhtml:link rel="alternate" hreflang="{{ .Language.LanguageCode }}" href="{{ .Permalink }}" />{{ end }}{{ end }}{{ if not (.Params.noindex | default false) }}
    <xhtml:link rel="alternate" hreflang="{{ .Language.LanguageCode }}" href="{{ .Permalink }}" />{{ end }}{{ end }}
  </url>
  {{- end -}}
{{ end }}
</urlset>
