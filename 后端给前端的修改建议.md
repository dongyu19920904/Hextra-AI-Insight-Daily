📋 给前端 AI 的指令：后端已就绪，请验收并补全前端

  背景同步：
  后端代码已修复，现在会向前端仓库推送以下两类文件。请确保前端 Hugo 项目能正确识别并渲染它们：

   1. 每日博客：推送至 content/blog/daily/YYYY-MM-DD.md
   2. 每周周报：推送至 content/blog/YYYY-week-XX.md

  请执行以下“最小化修改”检查清单：

  1. 📂 目录与结构检查 (必须)
  请检查仓库中是否存在以下目录和文件，如果不存在请创建：
   * [ ] 目录: content/blog/daily/ (必须存在，否则后端推送可能会报错或无法归档)
   * [ ] 入口文件: content/blog/_index.md
       * 内容要求: 必须包含 title: "博客" 和 layout: "blog" (或 list)。
       * 目的: 确保 /blog/ 路径能被访问，且作为列表页显示。

  2. ⚙️ 配置文件检查 (必须)
  检查 hugo.toml (或 config.yaml)：
   * [ ] 菜单: 是否已添加 Blog 或 博客 到 menu.main？

   1     [[menu.main]]
   2       name = '博客'
   3       url = '/blog/'
   4       weight = 30 # 调整顺序

  3. 🤖 自动化部署检查 (关键)
  检查 .github/workflows/ 下的部署脚本 (如 deploy.yml)：
   * [ ] 触发条件: on: push: paths: 下是否包含了 content/blog/**？
       * 目的: 确保后端推送博客文章后，GitHub Pages 会自动重新构建发布。

  4. 🎨 样式与布局 (可选，建议最小化)
  如果不想写复杂的 CSS/HTML，请确认：
   * [ ] 你的主题 (Hextra) 是否能默认渲染 content/blog/ 下的 Markdown 文件？
   * [ ] 如果需要简单的列表页，确保 content/blog/_index.md 的 Front Matter 中没有设置 render:
     false 或其他隐藏属性。

  ---

  验证命令 (供参考):

   1 # 1. 模拟后端推送的文件
   2 mkdir -p content/blog/daily
   3 echo -e "---\ntitle: '测试博客'\ndate: 2025-12-22\n---\n测试内容" > 
     content/blog/daily/test.md
   4 
   5 # 2. 本地预览
   6 hugo server -D
   7 # 访问 http://localhost:1313/blog/ 确认能看到"测试博客"
